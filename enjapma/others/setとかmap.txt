vector <ll> p;

//vectorの中身を消す
p.erase(/*この中身はアドレス*/);
//例えば
p.erase(lb(p.begin(),p.end(),50));
//こうすると50以上の最初の要素を消す

//最後の要素を出力
p(p.back());
//p(p.end()) -> これはエラーになる

//最初の要素を出力
p(p.front());
//p(p.begin()) -> これはエラーになる

//i番目の要素を出力
p(p[i]);
//ポインタを利用するとこんな感じ
p(*(p.begin() + i))



