ll bit[500005];
ll bitsum(ll i){
    //1-indexed
    //sum of 1,2,...i
    ll s = 0;
    while(i>0){
        //p("hoi");
        s += bit[i];
        i -= i & -i;
    }
    return s;
}
void bitadd(ll b,ll x){
    //add x to place:b
    while(b <= n){
        //p("hoi");
        bit[b] += x;
        b += b & -b;
    }
}