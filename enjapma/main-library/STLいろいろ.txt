cin >> n;

    //tableの内容を0で初期化
    table.resize(n);
    for(int i=0;i<n;i++){
        table[i] = (2 * i + 2) % 9;
    }

    //一番最初に挿入
    table.insert(table.begin(), 100);

    //一番最後に挿入
    table.insert(table.end(), 200);

    //最初の要素・最後の要素の取得
    a = table.front(); b = table.back();

    //要素の削除
    table.erase(table.begin() + 4);